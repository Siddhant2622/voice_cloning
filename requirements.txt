# ============================================================
# VoiceGuard — Production Requirements
# Python 3.11+ compatible
# ============================================================

# --- PyTorch (install separately for GPU builds) ---
# GPU:  pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
# CPU:  pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# --- ML / Audio ---
transformers>=4.40.0
librosa>=0.10.0
soundfile>=0.12.1
sounddevice>=0.4.6
speechbrain>=1.0.0
onnxruntime>=1.18.0            # CPU fallback; use onnxruntime-gpu on GPU

# --- Scientific stack ---
numpy>=1.24.0
scipy>=1.12.0
scikit-learn>=1.4.0
pandas>=2.0.0
tqdm>=4.66.0
matplotlib>=3.8.0

# --- Web / streaming (production API) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
aiofiles>=23.0.0
httpx>=0.27.0                  # async HTTP for HF Hub

# --- Configuration & auth ---
pydantic>=2.0.0
pydantic-settings>=2.0.0       # BaseSettings with env file support
python-dotenv>=1.0.0

# --- Cloud / model hub ---
huggingface_hub>=0.22.0        # push/pull checkpoints from HF Hub

# --- Rate limiting ---
slowapi>=0.1.9                  # SlowAPI rate limiter for FastAPI

# --- Utilities ---
PyYAML>=6.0
click>=8.1.0
rich>=13.7.0

# ============================================================
# Resemble AI Inspired Training Stack
# ============================================================

# --- Resemble Enhance: AI speech denoiser for training data augmentation ---
# Cleans bonafide samples → richer training distribution + 2x data
# pip install resemble-enhance --upgrade
resemble-enhance>=0.0.1

# --- Multi-TTS synthetic data generators (for multi-generator training) ---
# Training on diverse generators is the #1 factor for generalization
# (inspired by Resemble AI training on 1000s of TTS systems)

# Google TTS (neural, well-known voices)
gTTS>=2.5.0

# Microsoft Edge TTS (HiFi-GAN neural — hardest to detect, great hard negatives)
edge-tts>=6.1.0

# Coqui TTS — VITS neural vocoder (open-source high-quality TTS)
# Generates some of the most realistic synthetic speech
TTS>=0.22.0

# --- Mamba-SSM (optional CUDA extension for faster temporal modeling) ---
# Pure-PyTorch S4-lite fallback is included automatically if not installed
# CUDA only: pip install mamba-ssm causal-conv1d
# mamba-ssm>=1.2.0  # uncomment if CUDA available
