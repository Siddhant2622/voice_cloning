# ============================================================
# Voice Clone Detection Prototype — Requirements
# Python 3.14 + CUDA 12.8 (RTX 3050)
# ============================================================

# --- PyTorch (install separately via nightly index) ---
# python -m pip install --pre torch torchvision torchaudio \
#   --index-url https://download.pytorch.org/whl/nightly/cu128

# --- ML / Audio ---
transformers>=4.40.0
librosa>=0.10.0
soundfile>=0.12.1
sounddevice>=0.4.6
speechbrain>=1.0.0
onnxruntime-gpu>=1.18.0       # GPU-accelerated ONNX inference fallback

# --- Scientific stack (mostly already installed) ---
numpy>=2.0.0
scipy>=1.12.0
scikit-learn>=1.4.0
pandas>=2.0.0
tqdm>=4.66.0
matplotlib>=3.8.0             # DET curve plotting

# --- Web / streaming ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
aiofiles>=23.0.0

# --- Utilities ---
pydantic>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0
click>=8.1.0
rich>=13.7.0                  # pretty CLI output
